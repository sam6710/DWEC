// let array = [
//     [],
//     [],
//     [],
//     [],
//     []
// ];

Dimension = prompt("Introduzca la dimensión que desee para la matriz:");

treasureCreate(Dimension);

function treasureCreate(dimension){
    matriz = new Array(dimension);
};

function randomGen(min, max){
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min) + min);
};

function rellearMatriz(){
    if(){

    }
    else{

    }
};

// fila = randomGen(1,5);
// columna = randomGen(1,5);
// matriz[fila][columna]





// min = Math.ceil(min);
// max = Math.floor(max);
// return Math.floor(Math.random() * (max - min) + min);



